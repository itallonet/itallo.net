const MetaData = ({ date }) => {
    return (
        <section id="metadata" style={{ marginTop: '20px', display: 'flex', justifyContent: 'center', width: '100%' }}>
            {date}
        </section>
    )
}

export default MetaData;